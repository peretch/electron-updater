#productName: "software-vault-2"
#appId: "dev.skydiver.software-vault-2"

#electronVersion: "6.0.10"

asar: true

appId: "com.abya.abya-client"

files:
  - "main"
  - "build/next"

directories:
  output: "build/dist"

mac:
  category: "public.app-category.games"
  icon: "build/logo.icns"
  target:
    - target: "dir"
win:
  # icon: "build/logo.ico" #Comented becouse must be at least 256x256
  target:
    - target: "dir"
linux:
  category: "Game"
  icon: "build/logo.png"
  target:
    - target: "dir"